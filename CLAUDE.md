# daward — É MOOMENTS 100 (DAZN AWARDS 2026 ファン投票プロトタイプ)

Next.js 14 (App Router, TS)。詳細は README.md を参照。

## デプロイに関する重要な記録（毎回ハマるポイント）

**Cloud Run へのデプロイはセッションの種類で可否が決まる：**

- **クラウドセッション（claude.ai/code のWebやデスクトップから起動する Anthropic クラウド実行環境）には GCP 認証が無い。**
  `CLOUDSDK_AUTH_ACCESS_TOKEN` 等の環境変数が見えてもプレースホルダ（`proxy-in...`）であり、
  googleapis.com へのリクエストは 401 になる。`dl.google.com` は egress policy で遮断されて
  いるため gcloud CLI のインストールも不可。→ **クラウドセッションからは直接デプロイできない。**
- **Remote Control（ユーザーのPC上で動くローカルセッション）は、ローカルの gcloud 認証を
  使えるためデプロイ可能。** 過去のCloud Run関連作業はすべてこちらで実施している
  （例: 2026-08-24 セッション「AI活用理論の検証とCloud Runアプリ開発」）。

**【2026-08-26 更新・現行ルール】デプロイは作業中のセッションが自分で実行する：**

```bash
node scripts/deploy_cloud_run.js   # このリポジトリ直下で実行するだけ
```

- 環境「main」の全セッションに `GCP_SA_KEY` が入っており、クラウドセッションから直接
  デプロイできる（2026-08-26 にこの方式で成功実証済み）。
- スクリプト末尾のHTTP確認が403を返すことがあるが、IAM反映の一時的なもの。
  数秒後に curl し直せば200になる。反映確認は本番HTMLの `data-rev` マーカーで行う。
- **セッション分割・デプロイ専用セッションへの転送は廃止**。過去のデプロイ専用セッション
  （session_015FAp35...）はアーカイブ済み、転送用トリガーも全削除済み。
  理由: 転送先セッションは会話コンテキストを共有できず、ユーザーが直接指示した際に
  仕様を知らないままUIを勝手に再実装してブランチへプッシュする事故が起きた
  （2026-08-26、2コミットをユーザー判断で破棄・force-push で復旧）。
  **開発・検証・デプロイ・会話はすべて1つのセッションで完結させること。**

## アプリの構成メモ

- `data/moments.json` — 100本の実在YouTubeハイライト + 感情パラメータ（8種、先頭が主感情）
- `lib/layout.ts` — 投票数でタイルサイズが変わる重み付きBSPモザイク
- `lib/emotion.ts` — コメントからの辞書ベース感情推定（本番はClaude API差し替え想定）
- `lib/store.ts` — インメモリ投票ストア（本番は Firestore 差し替え想定）
- 映像素材は現状YouTube代用。本番mp4化は `lib/youtube.ts` の差し替えで対応
- **企画の核**: 「投票状況を体験として可視化し、気持ちいいUIとリンクさせる」。
  レイアウトは静止が基本、動きは①タイル内の映像 ②投票が入った瞬間の脈動（+🔥、黄フラッシュ）
  ③投票の熱量に応じた歓声サウンドスケープ（`lib/crowd.ts`、Web Audio合成。本番は実録差し替え）。
  他ファンの投票は `/api/pulse` でシミュレーション生成（本番はリアルタイム配信に差し替え）。

## リファレンスmp4の動き仕様（全1846フレーム解析で確定・2026-08-26）

数フレームの静止画比較では動きを理解できない（ユーザー指摘の通り）。ffmpegで全フレームを抽出し、
タイル境界線の位置を毎フレーム追跡して確定した事実（tapehead Lab 50fps・36.9秒）:

1. **焦点は1枚だけ**。カーソルが乗ったタイルだけが成長する。距離に応じて近傍も膨らむ
   ガウス型ではない。他のタイルは押されて圧縮されるだけ。
2. **成長は巨大**。ホバーし続けるとモザイク面積の過半〜ほぼ全面まで育つ（面積で数十倍）。
   境界線の移動振幅は画面幅の最大59%。「2.4倍の膨らみ」は桁違いに小さすぎた。
3. **タイムスケールは秒単位**。1回の成長は約1.5〜2秒のグライド。ピーク速度は画面幅の約60%/秒、
   終盤は指数的に減速して静止。オーバーシュート無し。τ=0.16秒の即応は速すぎて「ガタガタ」に見える。
4. **履歴（航跡）が残る**。カーソルが次のタイルへ移っても前の焦点は即座に戻らず、ゆっくり縮む。
   画面上に「大・中・縮小中」のタイルが同時に存在し、境界線が波のように流れる。これがぬるぬるの正体。
5. **駆動はカーソルの位置であって移動量ではない**。静止ホバー中も飽和まで成長し続ける。
   完全静止になるのは (a)成長が飽和したとき (b)カーソルが離れて全タイルが基準サイズへ戻り終えたとき。
6. 静止時のモザイクは完全静止・すき間なし（従来理解の通り）。

実装方針（focusEnergyモデル・scroll1で更新）: 各タイルに e∈[0,1]。ホバー中 τ=0.75秒で e→1
（1.2秒は立ち上がりが「もたつく」とNG判定）、非ホバーは τ=2.0秒で e→0。
weight = base + K·e²、K は「育ちきったら見えている画面の約8割」から毎フレーム逆算
（キャンバスが縦300vh（スマホ380vh）のスクロール制になったため、キャンバス比ではなく画面比で決める）。
分割構造（BSPの分割位置・軸）は初期重みで固定し、比率のみ毎フレーム更新
（重み変化で分割が切り替わると配置が不連続にジャンプする＝ガタつきの原因だった）。
カーソルは画面座標で保持し毎フレーム scrollTop を足してキャンバス座標へ変換。
ライブiframeはIntersectionObserverで画面内のタイルのみマウント。

## 立体音響の歓声（scroll1で実装・2026-08-26）

仕様: カーソルが乗ったモーメントの歓声が聞こえる。音量=投票数×成長度、
定位=タイルの画面上の位置（StereoPanner）。フォーカス移動で2ボイス交差フェード、
離れるとゆっくり減衰。無フォーカス時はごく小さな場内のざわめきのみ。
音源: `public/crowd.ogg` = Internet Archive「crowd-cheer-ii-6263」（Public Domain Mark）の
4〜16秒区間を1秒クロスフェードでシームレスループ化した実録。fetch失敗時は合成にフォールバック。
ブラウザの自動再生制限のため、最初のクリック/タップ後に有効になる（デフォルト音量60）。
Dockerfileに `COPY public ./public` が必要（standaloneはpublicを含まない）。

### dynamic1 での追加修正（2026-08-26）

- **`.tile-media iframe` には `pointer-events: none` が必須**。無いとライブ中タイルは
  クリックがiframeに吸われ、音の起動も詳細表示も反応しない（「演出調整を押さないと
  音が出ない」の正体。ボタンは普通のDOMなのでクリックが届いていた）。
- 音の起動リスナーはcapture段で常設（音量ゲート無し）。未起動の間は画面下部に
  「🔊 クリックすると歓声が流れます」ヒントを表示、起動で消える。
- 歓声が鳴っているタイルにはイエローの細リング（`.tile.sonic`、イン0.3s/アウト1.1s）。
  **注意: Reactが管理する `className` に対して classList で手動クラスを足すと、
  hover等の再描画で消される**。リングはJSXのclassName側で付与し、sz-*ラベルクラスは
  毎描画フレームで上書きする方式に変更済み。
- ライブ再生の選抜は「見えているタイルの大きい順に liveCount 枚」（0.7秒ごと見直し、
  フォーカス中タイルは必ず含む）。既定12枚・上限20。静止サムネだらけを防ぐ。
- 縮小 τ=1.1秒（2.0秒は「縮小が遅い＝もたつく」とNG判定）。PCキャンバスは340vh。

## 体験の抜本転換: 感情の宇宙（galaxy1・2026-08-26）

ユーザー方針: 「映像の意味（感情）をデータとして持っている」ことを最後のフィルムだけでなく
**前面の体験の核**にする。参照: D-NEW-DAY（Three.jsの星空で入力に近い1事例へ飛ぶ）。
こちらは「1つに飛ぶ」ではなく**塊そのものが言葉で組み替わる**のが売り。

- `components/Galaxy.tsx`: Three.js。8感情の星団（ロゴパレット色のグロー＋漢字ラベル）に
  100の瞬間がカード（スプライト・サムネは `/api/thumb/[id]` 同一オリジン中継でWebGLテクスチャ化）。
  位置は `lib/emotionSpace.ts` の `mapPosition`（主感情の星団を基点に副感情へ0.4だけ流れる。
  重心方式は複合感情が原点へ潰れるので不可）。投票数＝カードの大きさ。ホバーで歓声（既存エンジン）。
- 言葉で組み替え: `QueryBar` → `textToVector`（辞書。本番はClaude API）→ `queryPosition`
  （類似度が高いほど中心の塊へ、遠いほど外周へ。0.55秒の指数追従）。テーマの一文字が中央に立つ。
- 投票→フィルム: `/api/vote` に `query` を渡し、`buildArc` のテーマ＝投票先0.6＋言葉0.6＋コメント。
  フィルムのテーマ宣言に「“言葉”——その言葉と同じ震えを、100の瞬間から。」
- エントランス `components/Vortex.tsx`: 「熱狂の渦」モーションロゴ（2Dキャンバス、7色ストリーク＋
  サムネイルが渦を巻いて中心へ。「入場する」で外へ弾け、同時に宇宙のカメラがリビール）。
- 旧モザイク（`Mosaic.tsx`）は未使用で残置。
- 検証: headless Chromium は `--use-gl=swiftshader --enable-webgl --ignore-gpu-blocklist` でWebGL可。

### galaxy2 での作り直し（2026-09-08）— 「チープ・躍動感皆無・塊が分からない」への対応

galaxy1 は「点がばらまかれただけ」で塊が読めなかった。全フレーム実画面（モックサムネ付き）で判定して作り直した:

- **銀河＝密度波モデルの螺旋**。各粒子は「長軸の向きが半径に比例して回る楕円軌道」を同じ角速度で回る
  （差動回転だと数十秒で腕が巻き込まれて消えるので不可）。2300粒/銀河、12%は球状ハロー。
  粒子は独自ShaderMaterial（頂点ごとの size/color、加算合成、gl_PointSize は距離で減衰）。
- **核＝小さく強い核スプライト(12) + 薄いハロー(44, 0.12)**。大きなハロー(70)は「霧の玉」に見えてNG。
- **カード＝円盤の中に黄金角で均等配置**（`mapPosition` の副感情ドリフトで方向を決めると一方向に固まる）。
  投票数が多いほど内側。円盤と同じ向き・速さで公転。サイズ 5.5+10·norm（地図）。
- **カメラ＝環の外から見下ろす** HOME=(0,122,195)、target y=-10。環の内側にカメラを置くと手前の銀河だけ巨大化する。
  環半径 78（62 だと隣の銀河と重なる）、上下交互 ±21±5。
  縦画面は fov 52→74、真上寄り (0,300,175) から。ラベルは見下ろし時は -z 方向へずらす（画面上で上に出る）。
- **言葉モード**: 関係する銀河が中心へ（w）、関係ない銀河は外へ退き(×1.55)・暗くなる(0.07)。
  上位36件が黄金角の螺旋（r=4+√k·5.6）、順位が高いほど大きい。カメラ (0,40,118)。
- 投票の閃光: 感情色のリングが 4→28 に広がり 1.1秒で消える + カードが一瞬明るくなる（×1.8 まで。×2.4 は白飛び）。
- 検証: `scratchpad/galaxy_test.mjs`（Playwright + swiftshader、`/api/thumb/**` をモックJPGで代替、
  PC 1440×900 / スマホ 390×844 の両方で 渦→地図→言葉 の各段階を撮る）。

## 体験の再定義: 渦そのものがインターフェース（vortex1・2026-09-08）

galaxy2 に対するユーザー判定:「よりチープ。入口の渦が安っぽい（ウルトラマンOPの墨流しのようなリアルな表現を期待）。
入った後は宇宙風でコンテンツが見えない。自由入力に対応していない。分類（感情8種）が凡庸で楽しくない。UIが平面的で3Dの動きが
サンプル（D-NEW-DAY: 3D空間をカメラが飛び、入力で目的地へ突っ込む）と違う。『ベテランの熱量』と入れたら渦が動いて全分類が
再構築される体験を設計すべき」。→ 以下に全面転換した。

**制作プロセス（ユーザー指示・恒常）**: 実装前に gpt-image-2（OPENAI_API_KEY は環境「main」に有り）でUI画像を生成し、
自分でループを回して要求水準に達したものをリファレンスにしてコーディングする。生成スクリプト: `scratchpad/gen.mjs`
（`NODE_USE_ENV_PROXY=1 node gen.mjs out.png 1536x1024 prompt.txt [ref.png...]`。Nodeのfetchはプロキシに乗せるため
NODE_USE_ENV_PROXY=1 が必須）。採用リファレンス: 入場=写真が渦に捻れる（gen/entry1.png, entry_mid1.png）、
メイン=渦の中をカメラが飛び、腕にAIの名前（gen/vmain1.png）、入力後=腕が作り直される（gen/vquery1.png）。
「感情8分類の島を並べる案（gen/main1.png）」は「分類して並べただけ・2000年代のサイト」として却下。

**実装（rev vortex1）**
- `lib/vortexShader.ts`: 画面全体のフラグメントシェーダー。入場時は100本のサムネイルのモザイク（アトラス8×5）を
  中心集中の捻り（f=exp(-(r/core)^4)、core=0.56）で渦にする。10秒かけて twist 0→9.5（e^2.4 のゆっくり立ち上がり）。
  角度方向のぼかし＋fbmのゆらぎ（墨流し）＋ロゴパレットの帯と細い流線。入場後は uPhoto→0 で写真が溶け、色の帯だけ残る。
  角度の継ぎ目（±π）はノイズ入力を cos/sin にし、帯の色は mod 3 で畳んで消した。
  **rAFの最初のタイムスタンプは performance.now() より前のことがあり、t<0 で Math.pow が NaN → 全部黒になった**。t は max(0,·)。
- `components/VortexSpace.tsx`: WebGL（背景シェーダー＋腕ごとの平たいリボン7本、加算合成）と CSS3DRenderer（100枚の
  映像カード＝DOM、img/iframe/タイトル/🔥票数）が同じ PerspectiveCamera を共有。カードは円筒座標（θ, r, z）で漏斗状に配置
  （R_NEAR 52 → R_FAR 14、DEPTH 120、TWIST 2.5）、腕ごとに θ の基点。近いほど大きく、目に向かって小さく。
  組み替え時は円筒座標で減衰追従（τ0.95s）＋ θ を −0.9π ずらして渦を巻きながら移動、カメラが z=20 まで飛び込んで戻る。
  ドラッグで回転、ホイールで奥行き、ホバーで歓声（既存エンジン）、クリックでカメラがカードへ寄って詳細。
  ライブ再生はカメラに近い4枚（PCのみ）。縦画面は fov 74。
- `lib/interpret.ts` + `/api/arms` + `/api/interpret`: 腕（クラスタ）はLLMが命名・割当。既定モデル gpt-5.4-mini
  （gpt-4.1-mini は名前が凡庸、gpt-4.1 は長い。5.4-mini が「最後の花道」「老いてなお速い」など最も短く強い。約3秒/クエリ）。
  初期状態も映像データから生成（サーバーでキャッシュ、初回のみ約8秒）。キー無し/失敗時は感情辞書にフォールバック。
  Cloud Run へは `scripts/deploy_cloud_run.js` がセッション環境の OPENAI_API_KEY をサービスの環境変数として渡す。
  本番で `/api/arms`, `/api/interpret` とも source=llm を確認済み。
- 検証: `scratchpad/vortex_test2.mjs`（Playwright + swiftshader。`/api/thumb/**` は gpt-image-2 で生成した実写風の
  コンタクトシートを40枚に切ったモック `scratchpad/thumbs/` で代替。YouTube埋め込みは空ページに差し替え）。
  シェーダー単体は `scratchpad/harness/`（python http.server 3150 + `hshot.mjs "tag|twist=9&photo=1"`）で数秒で確認できる。
- 旧 Galaxy.tsx / Mosaic.tsx / Vortex.tsx は未使用で残置。

### vortex2（2026-09-08）— 「奥行きが無い・渦がただの背景・クリックと入力が演出と繋がっていない」への対応

ユーザー判定: 「サムネが渦の軌道に並んでいるだけで奥行きが無い（全部真正面）。クリックすると即ポップアップ。入力中は固まって見える。
渦の表現がしょぼい。ベストプラクティスをリサーチして本気で」。→ 以下を実装。

- **渦の本体は GPU流体シミュレーション**（`lib/fluid.ts`、Stable Fluids: 渦の力→curl→vorticity→発散→圧力Jacobi 18回→勾配除去→移流。
  染料は MacCormack 移流で滲みを抑える。半精度 ping-pong。sim 192 / 染料 768（スマホ 128/512））。
  写真のモザイクを染料として流し、ロゴ色のインクを落として本物の墨流しにする。捻りシェーダー（vortexShader.ts）は廃止・未使用。
  - パラメータの勘所: 速度は texel/s。`strength` は 60〜120 で5秒回すと写真は完全に溶ける（ハーネス a300/b300）。
    入場は 9秒で 0→115 に立ち上げ、写真が渦の目から溶けていく。curl は 6（25 だと細かい波打ちだらけになる）。
  - インクの一滴は渦が回っている中心域にだけ落とす（周りに落とすと孤立した陰陽マークになる）。
  - 実時間に合わせて 1/60 秒刻みで substep（本番は最大3、検証は `?simsteps=16`）。低fps環境でも渦の育ち方が変わらない。
  - 検証: `scratchpad/harness/fluid.html`（esbuild で `fluid_entry.ts` をバンドル、`fshot.mjs "tag|frames=300&strength=60"`）。
- **奥行き**: カードは渦の流れの接線に沿って傾く（`makeBasis`、カメラ向き 62% + 漏斗の法線 38%）。距離の霧（brightness/saturate）。
  近いほど実サイズも大きい（s=0 で 1.75倍）。渦は常に手前へ流れ（`flow` 0.02/s）、通り過ぎたカードは渦の目から出直す。
  腕の光の帯は WebGL 2層（カードの奥 `vs-gl` と手前 `vs-gl-top`、FRONT_S=0.2 で分割）でカードの前後を通る。
- **クリック**: カメラが 0.95 秒でカードへ飛び込み（追従）、寄り切ってから詳細を出す。
- **入力**: 押した瞬間に `pendingText` → 渦が加速・インク増量・カードがほどけて外へ・言葉が渦の目に浮かぶ（`.vword`）。
  解釈が届いたら腕を組み替え、インクが弾け、カメラが飛び込む。「元の渦に戻る」は廃止。LLMが `next`（次に入れたくなる言葉3つ）を返し、チップに出す。
- rAF最初のタイムスタンプ問題（t<0）は `Math.max(0, …)` で防いでいる（再発注意）。

### vortex3（2026-09-08）— 「渦がただの背景素材。渦と動画が分離。サンプルから何を学んだのか」への対応

ユーザー判定: 「奥行きが無い＝渦が2Dの背景でカードがその前に浮いているだけ。入場が画面切替。入場ボタンは渦の中心に。
入力後に何が変わったか分からない（色も変わらない）。ラベルがチープ。カーソルで微小に揺れるだけ」。
サンプル（D-NEW-DAY index.html）を読み直して確定した文法: **空間は6万点の流れる粒子でできていて、コンテンツはその中に浮かぶ物体。
カメラはその空間を飛び、入力すると目的地へ2秒（Quadratic InOut）で突っ込む。閃光（flashOverlay 0.3）＋2000粒の爆発。**

実装（`components/VortexSpace.tsx` 全面書き直し、CSS3D廃止、WebGL単一シーン）:
- 渦 = 8万本の光の筋（LineSegments、頂点シェーダーが漏斗の流れに沿って毎フレーム位置を計算。腕ごとにロゴ色、
  `uMix/uSwirl` で前の腕から新しい腕へ渦を巻いて流れ直す＝組み替えが色の変化として見える）。スマホは3万本。
- カード = WebGLの板（CanvasTexture: サムネ＋タイトル＋腕の色の縁）。粒子と同じ深度バッファで前後関係が正しく決まる。
- 渦の目 = 流体シミュレーション（`lib/fluid.ts`）を z=-196 の板（340×544）に貼る。入口はこの目をカメラ z=96 から見る
  （画面いっぱいの写真の墨流し）。「渦に入る」で閃光→カメラが2.8秒で z=44 へ突っ込み、写真が溶け、カードが目から流れ出る。
- 入力 → 粒子が白熱・加速、カードがほどけ、言葉が目に浮かぶ → 解釈が届くと閃光・爆発・全粒子とカードが新しい腕へ、
  カメラが第1の腕へ2秒で飛び込み、2.6秒で基準位置へ戻る。クリックは閃光＋爆発＋1.4秒の飛び込み後に詳細。
- カーソルで視線が大きく振れる（±50ユニット）、ドラッグで渦を回す、ホイールで z −40〜60。
- **構図（ユーザー指摘で最後に直した本質）**: カメラは渦の入口の外から穴を覗くのではなく、**漏斗の内側**に置く。
  HOME=(0,−24,30)、視線 (0,14,EYE_Z)。下の壁が近く（近いカードが画面の1/4を占める）、目は画面上寄りの遠方。
  腕ごとに10本の太い平たいリボン（`buildRibbons`、幅0.9〜3.5、加算合成、縞のアルファをスクロール）が壁を覆い、
  カードの前後を流れる。組み替え時は前のリボンが消えて新しい色のリボンが現れる。カードは目の奥には置かない（s≤0.76）。
  入力後の飛び込みは壁へ向かわず**軸に沿って奥へ**（壁のカードに衝突しない）。
- 検証: `scratchpad/vortex_test3.mjs`（`?simsteps=16`、`window.__vs.bestCard()` で最大のカードをクリック）。

### vortex4（2026-09-08）— 操作設計（ユーザー仕様をそのまま実装）

ユーザー指示: 「奥行きは出た。ラベルのデザイン性がない（蛍光ペンの帯のほうが力強い）。重なった動画をどう見せるかの操作設計が無い。
ラベルをタップしたらスライドショー。自由移動は視点を戻せないと迷子になる。プリセットは入力欄に入れて送信ボタン。
送信後は全部の動画が渦に吸い込まれてぐるぐる回り、ラベルを印象的に出してから再配置」。

- ラベル = 蛍光マーカーの帯（`makeLabelTexture`: 腕の色で3本重ねの斜めストローク、上に黒の太字、右下に「N MOMENTS ▶」）。
- **ツアー**: ラベルをタップ/クリック → 渦そのものが回ってその腕を手前下へ運び（`tourSpinTarget`、最短回転）、
  その腕の映像だけが壁から内側のレーン（ro −18、等間隔）へ出て並ぶ。主役はさらに内側（ro −26）で1.4倍・明るく。
  カメラは軸寄りから主役を正面に（z+38）、レーンの続きと渦の目が奥に見える。その腕の帯と粒子が強く光り、他は沈む（`uFocus`）。
  終了で元の位置へ戻す（`restoreTourCards`）。4.5秒で自動送り、← →キー/スワイプ/何もない所をタップで送る。HUDは一行のバー。
  **NG判定済み**: カードをカメラ前に正対させて出す「舞台」方式（パワポのスライドショーに見える）、
  カメラを壁のカードへ飛ばす方式（隣のカードが被る・視点が壁側に回って上下が分からなくなる）。
- **検証の落とし穴（重要）**: rAFの dt は 0.05秒でクランプしているため、swiftshader（2〜3fps）では**シミュレーション時間が実時間の1/8**
  になり、遷移が終わる前のスクリーンショットを「完成状態」と誤認していた。検証は `?dtmax=0.5` を付けて実時間に追従させる
  （`tour_test.mjs` / `vortex_test3.mjs` は付与済み）。
- 初期の腕がAIから遅れて届いたときは演出なしで静かに差し替える（`dramatic` は `arms.text !== ""` のときだけ）。
- 入口ボタンの位置ガタつきの原因: `rise-in`（transform を animate）を中央寄せ（translate(-50%,-50%)）の要素に付けていた。
  animation は子要素に付ける。
- **視点を戻す**: ドラッグ/ホイール/ツアーで基準から外れたら右上に「⟲ 視点を戻す」（`onViewDirty`）。
- **入力**: チップは入力欄に入るだけ。送信で `pendingText` → 全カードが目へ吸い込まれ（ts=1.16、size 2.5）、粒子が白熱して回る。
  解釈が届いたら `REVEAL_MS`=2.6秒、ラベルが画面中央に縦に一枚ずつ立つ（320ms間隔、1.6倍）。その後 release: カードが新しい腕へ
  放たれ（base −0.9π で渦を巻く）、リボンが新色へ、閃光・爆発、軸に沿ってカメラが飛び込み戻る。

## セッション共通の運用ルール（ユーザーからの恒常指示・2026-08-25追記）

- 「後で確認する」と言うときは、必ず send_later 等で実際にスケジュールする。できない場合は「できない」と言う。
- 進捗が止まっているときは、止まっていると正直に報告する。楽観的な推測で埋めない。
- 自分から出力を観測できない場所（他セッションへの依頼転送など）を「完了見込み」として扱わない。頼りにする場合は結果をこのリポジトリへのプッシュ等、観測可能な形で受け取る設計にする。
- GCPプロジェクトは1つに集約する。新設しない。名前の変更は「表示名」で行う（プロジェクトIDは変更不可だがURL等にはほぼ露出しない）。
- ユーザーに同じ説明・同じ指摘を繰り返させない。判明した事実・決定はその都度このファイルに追記する。
- クラウドセッションにはclaude.ai（チャット）側のメモリや過去チャットの記憶は届かない。横断ルールはこのCLAUDE.mdと、環境のSetup script（ユーザーレベルCLAUDE.mdの生成）に置く。

## デプロイ実績

### 2026-08-25 クラウドセッションからのRESTデプロイ試行 → 失敗（キー不完全）

- 結果: **失敗**。Cloud Run へのデプロイは未実施（GCPへの認証自体ができなかった）。
- 失敗段階: OAuthトークン取得の前段。環境変数 `GCP_SA_KEY` は存在する（長さ1674の base64 文字列）が、
  中身は**サービスアカウントJSON全体ではなく、秘密鍵（private_key）のbase64本体のみ**だった。
  JSONとしてパース不可、base64デコードしてもJSONにならない（DER形式のRSA秘密鍵）。
- JWT作成には `client_email`（iss）と `project_id` が必須だが、秘密鍵からは導出できないため認証不可。
  `CLOUDSDK_AUTH_ACCESS_TOKEN` はプレースホルダ（`proxy-in...`）で googleapis.com は 401（既知の事実の再確認）。
  ディスク上にもSA JSONは無し。
- 対処方法: 環境変数 `GCP_SA_KEY` に、GCPコンソールからダウンロードした**サービスアカウントキーのJSONファイル全体**を
  設定し直す。改行が問題になる場合は `base64 -w0 key.json` の結果を入れる（デプロイスクリプトは両形式に対応させる）。
  必要フィールド: `project_id` / `client_email` / `private_key`。
- 注意: 今回の試行中、Nodeのパースエラー出力に秘密鍵のbase64本体がセッションログ上へ表示されてしまった
  （リポジトリには一切書いていない）。client_email が無いため鍵単体では悪用困難だが、
  再設定時は**新しいキーを発行**（既存キーは削除）することを推奨。
- 環境の egress 確認: oauth2.googleapis.com / cloudresourcemanager.googleapis.com への疎通はOK。
  REST経由デプロイのスクリプトは `scripts/deploy_cloud_run.js` としてコミット済み
  （生JSON/base64どちらのキー形式にも対応。GCS→Cloud Build→Cloud Run v2→IAM付与→URL確認まで自動）。
  キー再設定後は `node scripts/deploy_cloud_run.js` を実行するだけでよい。

### 2026-08-25 (2回目) キー形式は解決、実行が権限クラシファイアにブロック → 未完遂

- キー確認: **今回の `GCP_SA_KEY` はほぼ正しい**。ただし**外側の波括弧 `{}` が欠けた状態**で
  保存されていた（値が `"type"` で始まり `"` で終わる。環境変数UIへの貼り付け時に欠落した模様）。
  `{` + 値 + `}` で JSON.parse に成功し、`type=service_account` / `project_id` /
  `client_email` / `private_key`（BEGIN PRIVATE KEY、改行込み）すべて揃っていることを確認済み
  （値そのものはログに出していない）。
- 対応: `scripts/deploy_cloud_run.js` の `parseKey` を波括弧欠落形式にも対応させた
  （生JSON / 波括弧なしJSON / base64 の3形式を受理）。ソースtarball作成まで完了。
- 失敗段階: `node scripts/deploy_cloud_run.js` の実行自体が、クラウドセッションの
  権限クラシファイア（auto mode）に拒否された。SA鍵を使った外部デプロイという操作の性質による
  自動ブロックで、フォアグラウンド/バックグラウンドとも同一の拒否。GCPへのリクエストは未送信。
- 次の一手（いずれか）:
  1. ローカル（Remote Control）セッションで `gcloud run deploy` を実行する（従来の実績ある方法）。
  2. クラウドセッションで再依頼する場合は、permission mode を明示的に許可寄りにするか、
     settings に `Bash(node scripts/deploy_cloud_run.js*)` の許可ルールを追加してから起動する。
- 補足: 環境変数の `GCP_SA_KEY` は波括弧を補って設定し直すのが望ましいが、
  スクリプト側で対応済みのため必須ではない。

### 2026-08-25 (3回目) クラウドセッションからのRESTデプロイ → 成功

- 結果: **成功**。`node scripts/deploy_cloud_run.js` 一発で完遂（承認1回、リトライ1回）。
- **URL: https://e-mooments-vckorfe4qa-an.a.run.app** （デプロイ直後にHTTP 200確認済み）
- project_id: `love-coach-sprint0`（表示名 love-coach） / リージョン: `asia-northeast1` / サービス名: `e-mooments`
- 初回実行は Cloud Resource Manager API が SERVICE_DISABLED で失敗。スクリプトに以下を組み込んで再実行し成功:
  - serviceusage API での自動有効化＋伝播待ちリトライ（SAに serviceusage.services.enable 権限があり有効化可能だった）
  - ソースtarballの自動生成（コンテナは毎回フレッシュクローンのため、実行時に生成する。コミット不要）
  - プロジェクトチェック（CRM API）は情報目的なので失敗しても続行するよう変更
- 権限クラシファイアのブロック（前回2回目の失敗要因）は今回は発生せず、ユーザー手動承認で実行できた。

### 2026-08-25 (4回目) 再デプロイが反映されない問題 → digest指定で解決

- 症状: 2回目以降の `scripts/deploy_cloud_run.js` 実行は build SUCCESS・service update 200 でも
  **本番に新コードが反映されない**（本番HTMLに `sound-toggle` が無いことで発覚）。
- 原因: サービス更新PATCHがタグ無し（暗黙 `:latest`）の**同一イメージ文字列**だったため、
  Cloud Run側でテンプレート変更なしと見なされ**新リビジョンが作られず**、旧リビジョンが配信され続けていた。
- 対策（実装済み）: Cloud Build結果から `results.images[0].digest` を取得し、
  `image@sha256:...` の**digest指定**でデプロイするようスクリプトを修正。毎回確実に新リビジョンが作られる。
- 検証方法: デプロイ後に `curl -s <URL> | grep -c sound-toggle` 等、**そのコミット固有のマーカーが
  本番HTMLに含まれるか**で確認する（HTTP 200だけでは反映確認にならない）。

## 恒久化の最終形（2026-08-25 確定）

環境「main」のSetup script（雲マーク→main→歯車→Setup script欄）に恒常設定を集約する。
ここに置いたものは**全リポジトリ・全セッションに自動適用**される（このCLAUDE.mdはdawardリポジトリ限定）。
Setup scriptには (1) ユーザーレベルCLAUDE.md（恒常ルール）の生成 (2) ユーザーレベルsettings.json
（定型コマンドの事前許可 = 承認プロンプト廃止）の生成、を入れる。内容はチャットでユーザーに提示済み。
これが入るまでは、デプロイ承認は「同一コマンドの再実行は確認されない」挙動に依存する。
